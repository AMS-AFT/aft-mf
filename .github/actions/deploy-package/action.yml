name: Deploy Package
description: Deploy project package

inputs:
  project_name:
    description: project name
    required: true

runs:
  using: composite
  steps:
    - uses: actions/checkout@v3

    - name: Setup ${{ input.project_name }}
      uses: ./.github/actions/setup

    - name: Build ${{ input.project_name }}
      shell: bash
      run: pnpm exec nx run ${{ input.project_name }}:build --skip-nx-cache

    - name: Deploy ${{ input.project_name }} in npm.js
      shell: bash
      run: pnpm publish dist/packages/${{ input.project_name }} --access=public
